{
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "preview": "./gen-wasm-glue.sh && vite preview",
    "build": "./gen-wasm-glue.sh && vite build"
  },
  "devDependencies": {
    "vite": "^7.1.10",
    "vite-plugin-wasm": "^3.5.0"
  }
}
